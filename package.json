{
  "name": "backbone-deep-model",
  "description": "Improved support for models with nested attributes.",
  "author": {
    "name": "Charles Davison"
  },
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha -u bdd",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec",
    "coverage-html": "istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec"
  },
  "version": "1.0.0",
  "dependencies": {
    "backbone": ">=0.9.10",
    "istanbul": "^0.3.6",
    "jquery": "^2.1.3",
    "underscore": "^1.8.2"
  },
  "devDependencies": {
    "browserify": "^9.0.3",
    "buildify": "latest",
    "chai": "^2.1.0",
    "gulp": "^3.8.11",
    "gulp-rename": "^1.2.0",
    "gulp-uglify": "^1.1.0",
    "gulp-wrap": "^0.11.0",
    "mocha": "^2.1.0",
    "vinyl-buffer": "^1.0.0",
    "vinyl-source-stream": "^1.0.0"
  }
}
